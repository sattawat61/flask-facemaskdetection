from itertools import count


myList = ["Eren", "Mikasa", "Armin", "Levi"]
count = -1
for i in range(len(myList)):
    # count +=1                       
    break
for i in range(len(myList)):
            # count +=1                       
    break  
for i in range(len(myList)):
    count +=1                       
    print(myList[i],i,count)
    
